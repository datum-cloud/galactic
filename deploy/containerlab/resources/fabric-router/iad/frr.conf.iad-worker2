frr defaults traditional
hostname iad-fabric-edge1
log syslog informational

interface lo
 ip address 10.255.255.5/32
 # /128. This is the address galactic-router's outbound EVPN session to the
 # route reflector sources from, auto-detected from lo via
 # GALACTIC_ROUTER_BGP_LOCAL_ADDRESS.
 ipv6 address fc00:0:9::1/128
!
interface bond0
 description tr3-facing (transit uplink) -- LACP bond of eth1 + eth4
 ip address 10.1.32.2/24
 ipv6 address 2001:db8:1:32::2/64
!
interface bond1
 description iad-worker-facing (compute) -- LACP bond of eth2 + eth5
 ip address 10.1.30.1/24
 ipv6 address 2001:db8:1:30::1/64
!
interface eth3
 description iad-worker3-facing (route reflector)
 ip address 10.1.31.1/24
 ipv6 address 2001:db8:1:31::1/64
!

# Anycast ingress VIP aggregate -- see
# resources/fabric-router/dfw/frr.conf.dfw-worker2 for the full rationale.
# Originated identically by every edge node in the lab.
ipv6 route 2001:db8:6060::/48 Null0

# This node's egress shard (resources/galactic-nat/iad/egressshard.yaml),
# originated into the underlay: its SID's covering /64 and its two masquerade
# addresses. See resources/fabric-router/dfw/frr.conf.dfw-worker2 for the
# full rationale, including why the shard's own node originates them and why
# Null0 is safe here.
ipv6 route 2001:db8:ff03:2002::/64 Null0
ipv6 route 2001:db8:9966:2::/64 Null0
ip route 192.0.2.2/32 Null0

router bgp 65000
 bgp router-id 10.255.255.5
 no bgp default ipv4-unicast
 no bgp ebgp-requires-policy
 bgp log-neighbor-changes
 # eBGP to the transit. This node is one of the lab's only
 # transit-facing nodes -- compute and the route reflector have no
 # session to AS 65100 at all, and reach it through the iBGP
 # sessions below instead.
 neighbor 2001:db8:1:32::1 remote-as 65100
 neighbor 10.1.32.1 remote-as 65100
 #
 # iBGP to the workers sitting behind this node.
 neighbor 2001:db8:1:30::2 remote-as 65000
 neighbor 10.1.30.2 remote-as 65000
 neighbor 2001:db8:1:31::2 remote-as 65000
 neighbor 10.1.31.2 remote-as 65000
 !
 address-family ipv6 unicast
  neighbor 2001:db8:1:32::1 activate
  neighbor 2001:db8:1:32::1 allowas-in 1
  neighbor 2001:db8:1:30::2 activate
  # next-hop-self force: a path this node learned from the transit
  # carries the transit router's own address as next hop, which the
  # worker behind this node has no route to. "force" is load-bearing --
  # plain next-hop-self is not applied to *reflected* paths, because a
  # route reflector preserves the originator's NEXT_HOP by design
  # (RFC 4456 s.10). Without it, iad-worker learns the reflector's
  # loopback with iad-worker3's own link address as next hop, a subnet
  # it is not on, and resolves it through the Kind management
  # interface instead -- leaving the EVPN session permanently Active.
  # route-reflector-client: iBGP split horizon would otherwise stop
  # this node passing one client's prefixes to another (iad has two).
  neighbor 2001:db8:1:30::2 next-hop-self force
  neighbor 2001:db8:1:30::2 route-reflector-client
  neighbor 2001:db8:1:31::2 activate
  # next-hop-self force: a path this node learned from the transit
  # carries the transit router's own address as next hop, which the
  # worker behind this node has no route to. "force" is load-bearing --
  # plain next-hop-self is not applied to *reflected* paths, because a
  # route reflector preserves the originator's NEXT_HOP by design
  # (RFC 4456 s.10). Without it, iad-worker learns the reflector's
  # loopback with iad-worker3's own link address as next hop, a subnet
  # it is not on, and resolves it through the Kind management
  # interface instead -- leaving the EVPN session permanently Active.
  # route-reflector-client: iBGP split horizon would otherwise stop
  # this node passing one client's prefixes to another (iad has two).
  neighbor 2001:db8:1:31::2 next-hop-self force
  neighbor 2001:db8:1:31::2 route-reflector-client
  # /128, not the site /48 -- a per-node peering loopback is the only
  # thing meant to cross the transit as a host route.
  network fc00:0:9::1/128
  # Anycast ingress VIP aggregate -- see this file's own comment above.
  network 2001:db8:6060::/48
  # This node's egress shard's SID /64 and IPv6 masquerade address -- see
  # this file's own comment above.
  network 2001:db8:ff03:2002::/64
  network 2001:db8:9966:2::/64
 exit-address-family
 !
 # IPv4 underlay -- this node's /32 loopback, plus its egress shard's
 # NAT64 masquerade address. There is no IPv4 counterpart to the SRv6
 # locator block, and the ingress VIP aggregate stays IPv6-only: this lab's
 # NetworkRule VIPs are IPv6, matching the edge datapath's own scope (see
 # edgedsr.c).
 address-family ipv4 unicast
  neighbor 10.1.32.1 activate
  neighbor 10.1.32.1 allowas-in 1
  neighbor 10.1.30.2 activate
  neighbor 10.1.30.2 next-hop-self force
  neighbor 10.1.30.2 route-reflector-client
  neighbor 10.1.31.2 activate
  neighbor 10.1.31.2 next-hop-self force
  neighbor 10.1.31.2 route-reflector-client
  network 10.255.255.5/32
  # This node's egress shard's IPv4 (NAT64) masquerade address. A NAT64
  # reply arrives from an IPv4-only destination, so unlike tenant IPv4 it
  # does not ride inside SRv6 and does need a route in the transit FIB.
  network 192.0.2.2/32
 exit-address-family
!

ip forwarding
ipv6 forwarding
