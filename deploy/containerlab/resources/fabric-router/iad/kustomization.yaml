namespace: galactic-system
resources:
  - ../base

# One key per fabric-carrying node (iad: compute, edge, route reflector)
configMapGenerator:
  - name: fabric-config
    files:
      - frr.conf.iad-worker
      - frr.conf.iad-worker2
      - frr.conf.iad-worker3
